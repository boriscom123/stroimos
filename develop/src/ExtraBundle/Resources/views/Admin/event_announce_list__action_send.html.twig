{% if admin.isGranted('EDIT', object) and admin.hasRoute('send') and object.status == 'new' %}
    <a href="{{ admin.generateObjectUrl('send', object) }}" class="btn btn-sm btn-default edit_link" title="Отправить приглашения">
        <i class="glyphicon glyphicon-send"></i>
        Отправить приглашения
    </a>
{% endif %}
