{#Пользователь {{  }} опубликовал#} Добавлен новый комментарий к материалу <a href="{{ comment.thread.permalink }}">{{ publication.title }}</a>:

<p>Тема:<br>{{ comment.subject }}</p>
<p>Текст:<br>{{ comment.body }}</p>

<a href="{{ url('admin_fos_comment_comment_edit', {id: comment.id}) }}">Перейти к редактированию</a>
