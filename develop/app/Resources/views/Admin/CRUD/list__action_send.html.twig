{% if admin.isGranted('EDIT', object) and admin.hasRoute('send') and object.status == 'new' %}
    <a href="{{ admin.generateObjectUrl('send', object) }}" role="btn_send" class="btn btn-sm btn-default edit_link" title="Отправить">
        <i class="glyphicon glyphicon-edit"></i>
        Отправить
    </a>
{% endif %}
